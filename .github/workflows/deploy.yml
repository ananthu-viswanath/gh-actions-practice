name: Deploy Project
on: [push, workflow_dispatch]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v6
      - name: Install NodeJS
        - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm ci
      - run: npm test